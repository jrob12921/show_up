# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: aee7a5d60b699e97103233a028191d5a88d544a1aac3c30aa1cb9e75d07c2b10626d1514381a2cca9ad129ea1db74eb1cd08afb27447e89f74657f5fad5a48e0

test:
  secret_key_base: 8319a48086ba63dcf9161a4ee6462b6af09bdec9541bd51a3d640caf88014363e8c9bf41d76a469a5fa2a3260c3d06c72da62b072b798d7f029cde5b831eb602

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>

default: &default
  jambase_api_keys: 
    - <%= ENV["JAMBASE_API_KEY"] %>,
    - <%= ENV["JAMBASE_API_KEY2"] %>,
    - <%= ENV["JAMBASE_API_KEY3"] %>,
    - <%= ENV["JAMBASE_API_KEY4"] %>,
    - <%= ENV["JAMBASE_API_KEY5"] %>,
    - <%= ENV["JAMBASE_API_KEY6"] %>,
    - <%= ENV["JAMBASE_API_KEY7"] %>,
    - <%= ENV["JAMBASE_API_KEY8"] %>,
    - <%= ENV["JAMBASE_API_KEY9"] %>
