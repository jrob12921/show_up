$(".attend_button_container").append('<%= link_to("Unattend", unattend_path(@event.id), method: :delete, class: "large button unattend", remote: true) %>')


$(".attend").on("ajax:complete", function(e){
  $(".attend").hide()
})

